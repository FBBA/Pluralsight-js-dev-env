environment:
 matrix:
 # node.js
 - nodejs_version: "6"

 install:
  - ps: Install-Product node $env:"6"
  - npm install

 test_script:
   - node --version
   - npm --version
   - npm test

build: off
